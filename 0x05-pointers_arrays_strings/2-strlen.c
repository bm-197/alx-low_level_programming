#include "main.h"

/**
 * _strlen - string length counter
 * @s: parameter
 * Return: length of the string
 */

int _strlen(char *s)
{
	int count = 0;

	for ( ; *str != '\0'; str++ )
	{
		count++;
	}
	return (count);
}

